/*
 * Mandatory:
 *
 * vars.volume.class
 * vars.volume.storage
 *
 */
bb-volumes-yaml(parent, vars) ::= <<
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: keycloak-data-1
  namespace: keycloak
  labels:
    group: keycloak
  annotations:
    volume.beta.kubernetes.io/storage-class: <vars.volume.class>
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: <vars.volume.storage>

>>
